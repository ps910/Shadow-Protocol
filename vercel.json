{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "ignoreCommand": "node -e \"if (process.env.VERCEL_GIT_COMMIT_REF === 'gh-pages') process.exit(0); else process.exit(1);\"",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
